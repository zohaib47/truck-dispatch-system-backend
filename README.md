# Truck Dispatching System - Backend

Ye is project ka backend server hai.

## Tech Stack
* Node.js
* Express.js
* MongoDB (MERN Stack)

## Setup Instructions
1. Run `npm install` to install dependencies.
2. Create a `.env` file and add your MongoDB URI.
3. Run `npm start` or `npm run dev` to start the server.